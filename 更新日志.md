# 更新日志

不咕鸟每一版改了什么。**最新的在最上面。**

用得上的话：报 bug 时带上版本号（设置 → 账号最底下那行），
我才知道你手上跑的是哪一份 —— 代码早改了而你还在用上一个 exe，
这种事发生过。

---

## 0.4.0 「不咕之狂潮」 · 2026-09-01

**这一版专心改写作体验**，外观和操作为主，没堆新功能。

### 稿纸

- **打字机模式**，横竖各一个开关。竖向是当前行永远停在屏幕中间，
  往下写时纸在动、眼睛不动；横向是当前列停在水平中央，稿纸横着走。
  （横向会**关掉自动折行** —— 折行时一行永远填不满，光标就永远走不到右边，
  两者是硬冲突。设置里那句话说的就是这个。）
- **智能替换**：打 `"` 出中文弯引号、`--` 变破折号、`...` 变省略号，
  还能自己加「打什么 → 变什么」。直角引号「」是可切换的一档。
  三条规矩：只在你敲下那一刻替换、**绝不动文件里已有的字**；
  撤销一次退回你原本打的那个字符；能一键全关。
- **专注模式**：光标所在那一段之外的字变淡。**一段就是一行** ——
  中文小说不空行分段，所以不需要靠空行去分；一段折成几行时整段都亮着。
  焦点不在稿纸上时（在翻侧边栏、在看设定集）一个字都不淡。
- **上下留白、首行缩进**可调了（缩进能调成 0）。它是显示效果，
  **文件里一个空格都不存**。
- **顶栏显示这一坐的产出**：写了 1,240 · 删了 380 · 净 +860。
  三个数都摆出来，因为改稿那天净值常常是负的 —— 而删掉三百字不是没干活。
  点一下清零重新计。
- **便利贴折起来变成一个小名牌**。原来折叠只收正文、头部照样占一整张卡的宽度，
  贴五六张就把稿纸糊住了。

### 外观

- **外观设置在书架页也有一份**了，跟写作页那份是同一个组件、同一份配置。
- **内置主题砍到三档**：纸白、护眼、夜间。摆成三个色块，**色块本身就是预览**。
  砍掉的纸黄和墨灰不是藏起来了，是删了 —— 想要那种用自选 CSS 能做得更合你意。
  （原来用墨灰的，升级后会自动落到夜间，不会忽然变成一片惨白。）
- **可以导入自己的字体**（`.ttf` / `.otf` / `.woff2`）。文件会**复制**进应用数据目录，
  这样你哪天清理下载文件夹它也还在。用的是你自己机器上的字体，不咕鸟不分发字体。
- **每个主题各自记住字号**。夜间想大一号是常事，换回来不用再调一遍。
- **自定义主题栏位改成会长的一排，最多九个。**

  原来是三个固定格子 —— 三个是我拍的数。现在一开始只有一个空位，
  用掉它（导一份 CSS，或者存一套自己调的）之后**末尾立刻又长一个**，
  于是可以一直加，直到九个。空位本身就是「加」这个按钮，
  满了它自己消失。

  导进来的和自己调的**共用同一排**：一个色块、双击、右键改名、叉掉删除，
  操作完全一样，不用记得哪格是哪种。双击的行为按格子走 ——
  文件主题换一份 CSS，自制主题打开调色器接着调那一格。

  满九个时**不会挤掉最老的那一份**（那是你调了半天的配色），界面直说。
  删的是整格不是清空，删掉正在用的那格回预设，删掉它前面的序号往前挪 ——
  不然会悄悄换成别的主题。

- **加了调色器：自己调一套主题，不用再迁就别人的。**

  设置 → 外观 → 「自己调一套」。左边一列表选调哪一项，右边调那一项 ——
  **色盘、色号、RGB 三种输入都能用**，改一下当场看到效果。
  能调的就是不咕鸟认得的全部：底色、纸色、字色、强调色、
  悬停和选中、代码块、危险和达标、热力图五档、图表两色、两种阴影。
  正文字体也算这套主题的一部分。

  调好按「存到自定义栏」就占一格，跟导进来的主题摆在一起 ——
  **能存好几套，点一下就换**。

  调完也可以**导出成一份 .css**。那份文件带注释、带分组，是给人看给人改的，
  而且**用「自选样式」能再导回来**，也能直接发给别人 ——
  它跟不咕鸟认得的格式完全一致。

  几个不起眼但重要的地方：起点是当前那个预设而不是空白（从零调
  二十几个颜色没人受得了）；预览不存盘（拖色盘一秒几十个值，
  每个都存会卡到拖不动）；没按「用这套」就收起的话，
  屏幕会恢复成真正生效的那一份 —— 不然「看着是这样、其实不是」。

- **Typora 主题现在是真能用了**，不只是「装得上」。

  以前装一份 Typora 主题，往往只有滚动条之类的细节变了。原因查清了 ——
  一份主题里的规则大致是这样分的：一多半打向 Typora **自己的界面**
  （侧栏、标签页、菜单），我们没有那些东西；剩下的一大半打向真正的
  Markdown 元素（`h1`、`blockquote`、`p`），而不咕鸟的稿纸是个编辑器，
  里头压根没有 `<h1>`。所以它们一条都匹配不上。

  这一版做了三件事：

  1. **`@import` 会真的展开。** 很多主题的入口文件只有一行 `@import`，
     正文全在被引的那个文件里 —— 以前那一整份都静默丢了。
     顺带：被引的文件找不到时会**明说**，不再假装装好了。
  2. **稿纸的每一行都标上了「它是什么」**（标题、引用、列表、代码、正文）。
  3. **自动把 `#write h1` 这类规则翻译到那些行上。**
     拿一份实际的主题量了一下：**62 条排版规则翻了过来**，
     标题大小、引用样式、段落字体、表格线这些都跟着生效了。

  翻译**只追加、不改原文**，只碰 `#write` 底下的规则，翻不动就不翻。
  唯一一处会动主题内容的地方：会摘掉让整行脱离文档流的
  `position:absolute` 和 `float` —— 那会让光标乱飘。

  做不到的也说清楚：**行内的**加粗、斜体、链接样式翻不了（我们只能给整行贴类），
  Typora 自己那套界面的规则也永远不会生效。

- **装完给回执**：装进去多少 KB、改到了几个颜色、翻译了几条排版规则。
  三个数分开说 —— 颜色和排版是两条独立的路，合成一句话你就分不清是哪条断了。
  （顺带修了个一直在骗人的 bug：那句「定义了哪几个变量」的检查，
  正则少写了一个反斜杠，于是**永远**报「一个变量都没定义」。）
- **不咕鸟的主题变量对齐了 Typora 的命名**：`--bg-color`、`--text-color`、
  `--primary-color` 这些都是 Typora 那套名字，所以主题里写的颜色直接就能改到我们。
  Typora 没有的概念（热力图五档、图表网格线）才用我们自己的名字。
- **想自己写一份主题**：仓库里有 `主题模板/不咕鸟示例主题.css`，
  一张带注释的完整清单，每个变量管什么都写着，照着改就行。

### 导出

排版整块挪进了导出 —— **文件始终干净，空格只出现在导出的产物里**。

- **多了 md 一档**。md 保留语法（原样搬走、换个编辑器接着写），
  txt 不保留（排给人看）。两档的默认值是反的，因为用途是反的。
- **能选导哪几部分**：正文、大纲、设定集。选了不止一部分就导成一个文件夹，
  每部分一个文件。设定集导成**一个文件**，分类当二级标题。
- **中英之间加空格**、**收拾多余空格** 两个新选项。
- ⚠️ **补了个漏子：`@` 从前根本没被处理。** 也就是说 0.3 之前你导给编辑的 txt 里，
  行首那个 `@` 是原样带出去的。现在会去掉标记、留下那句话，而且能开关。

### 奖状

给征文、比赛之类留个纪念。**不是成就系统**：没有清单、没有条件、不会自动获得，
由我手动发。拿到之后显示在书架你的名字底下，楷体，2–6 个字。
有好几张的话点一下换一张。**只有你自己看得见**，不进公开接口。

### 修的 bug

- **拖人物卡到稿纸上会往正文里插一个人名。** 这是 0.3 就有的 bug：
  目录树拖便利贴时顺手放了一份 `text/plain`（卡片标题），
  而稿纸里坐着的编辑器认这个 —— 于是标题被当成正文插进了你的稿子，
  **不报错、不提示**。便利贴本来就是自由拖动的，没有吸附。

---

## 0.3.0 「咕咕之禁城」 · 2026-09-01

这一版**整版都是修 bug**，没加新的大功能。
十二条问题全部来自 **zhangls** 的反馈 —— 详见文末致谢。

### 稿纸

- **侧边栏钉住之后不会再掉了。** 关掉软件再打开，钉着的还钉着。
  以前钉住只活在内存里，每次开软件都得重钉一遍。
- **主题从两套扩到五套**：纸白（默认）、**护眼**（压过灰的豆沙绿）、
  纸黄（旧纸色）、墨灰（原来的深色）、**夜间**（近黑，关灯时用）。
  设置 → 外观里换。
- **可以自己挑一份 CSS 当稿纸样式，Typora 的主题文件直接就能用。**
  设置 → 外观 → 自选样式。它们排版正文用的是 `#write`，稿纸也顶着这个 id，
  所以字体、字号、行距、标题、引用、代码块会跟着变。
  侧边栏和按钮的配色仍归上面那个主题管 —— 一份为「一整个窗口就是文档」
  写的 CSS，套到有侧边栏的界面上只会更难看。
- **按 Ctrl+S 之后右上角浮一下**，告诉你距上次**手动**保存又写了多少字，
  外加本篇总字数。自动保存不弹 —— 那是三秒一次，弹起来能把人逼疯。
- **右键菜单不会再压住你刚选中的那段字了。** 稿纸上的菜单有十几条，
  从光标处往下开正好盖住选区，右键想复制时连「我选对了没有」都核对不了。
  现在它会自己挪开：先往右（选中那段还在原地，菜单摆它旁边），
  实在不行才往下、往上。
- **启动时只弹一个窗**：连胜多少天、今天写了多少字、上次写到哪一篇哪一行，
  底下两个按钮 ——「进入书架」和「回到上次」。回车就是回到上次。
  原来这是两个弹窗（先连胜、再问回不回去），第一个成了必须先点掉的门。
  只在刚打开软件、还没进过任何一本书时弹；退回书架时不会再冒出来。
- **行首 `@` 现在跟 `#`、`<!--` 一样顶格了**，而且那个 `@` 会上色 ——
  它是标记不是正文，原来跟正文一样缩两格、一样的颜色，一屏字里根本认不出来。

### 游戏剧本

- **路线图不用等了。** 以前要等自动保存那三秒图才动，看着就像卡住；
  现在改一个字它就跟着变 —— 拿的是编辑器里还没存盘的那一份。
- **新写法：合并 `<- 甲、乙`。**
  「分歧一下、后面还是同一条线」是分支剧情里最常见的形状，
  以前要跑去**每一条**分支尾巴上补一行 `-> 汇合点`：五条分支五个地方，
  加一条还得记得回来补第六个，漏一个就是一条死路。
  现在在合流的那个节点上写一句就够，它自己声明谁流进来。
- **新写法：一行写多个去处 `-> 图书馆、天台、社团`。**
  自动摊成三个选项，选项文字就是节点名。
  「这个时间段去哪儿」那种自由度高的写法不用再一条条抄。
- **结局写成 `-> 【结束】`。** 方括号里写什么都算结局名。
  这样你给节点起名叫「结束」也不会把整条线提前掐掉 ——
  以前 `-> 结束` 是保留名，撞上同名节点时整条线在跳过来的那一刻就没了，
  而你只会看到「我后面写的都走不到」。
  老写法仍然认，只是书里有同名节点时以节点为准。
- **有意绕回前面的节点，写成 `-> ↩节点名`。**
  没标记就绕回去的，体检会提一句 —— 那多半是两处不小心起了同一个名字。
  **这是提示不是错**：有意的循环（回到选择菜单、重复的日常）标一下就不提了。
- **变量那一块重写了。** 现在直接告诉你：怎么写（四条常用写法，还能一键
  插进稿纸）、每个变量在哪几个节点被改过（点一下跳过去）、
  哪些变量只在条件里出现过却从来没赋过值（用到它的分支永远走不到）。
- **默认的示例文本只在第一篇出现了。** 第二篇起只给一行标题，
  不用再先删二十行「李四」。剧本也一样。

### 顺手修的

- 一个会随机变红的测试（大量磁盘写入在 Windows 上偶尔超时）。
  它是假警报，但假警报会教人「红了先重跑一遍」，而那个习惯迟早会盖掉一个真 bug。

---

## 0.2.0 「不咕之境」 · 2026-08-28

**对外统计打通了** —— 别的网站可以读到你的连胜和字数了。

- 设置 → 账号 → 对外统计：登录后认领一个**对外短名**（比如 `mingting`），
  就有了一个任何人都能读的地址：
  `https://bugu.char46.top/api/v1/u/你的短名/stats`
  贴到博客、主页、群公告里，或者让别人的网站直接 `fetch` 它。
- **会发出去的就七个数**：今天写了多少、今天的底线、本周合计、当前连胜、
  最长连胜、一起写了多少天，加上日期。书名、章节名、正文一个字都不会离开这台电脑。
- 那七个数**一直摆在按钮上面，没登录时也摆着** —— 先看清楚再决定要不要登录。
- **不认领短名就没人读得到你**。认领这个动作本身就是「我确实想公开」。
- **自动上传默认关着**，得你自己打开（打开后每半小时一次）。
- 「看看别人读到什么」走的是公开接口、不带你的令牌，跟别的网站看到的一模一样 ——
  自己看自己的特权视图会骗人。
- 「删掉服务器上的我」按一下就删干净，连短名一起放回去。
  做不到彻底删除的公开功能不该上线。

不用这个功能完全没关系，**不登录整个软件一个功能都不少**。

---

## 0.1.0 · 2026-08-27（内部，没有公开发布）

第一个跑得起来的包，只在本机试过，**没有对外放过**。
列在这儿是为了让下面那些「以前怎么怎么样」有个出处 ——
0.2「不咕之境」才是第一个发出去的版本。

- **写**：稿纸、大纲、设定集、便利贴浮出、双链 `[[ ]]`、全文检索（中文分词）、
  版本历史（每次保存留一份，改错能翻回去）
- **盯着你写**：每日目标（工作日/休息日分开设，底线和理想线两条）、
  连胜、补签、请假、番茄钟、字数统计、年度热力图、里程碑
- **长篇专用**：伏笔（埋在哪、该在哪收、过期标红）、关联、灵感箱
- **剧本 / 游戏剧本**：剧本排版（只改显示，不动文件一个字节）、
  谁的戏多、场次重排、分支节点图、体检（断头路 / 孤儿 / 死路 / 走不到的分支）、
  从任意节点试玩、导出 Ren'Py / ink 骨架
- **AI**：兼容 OpenAI 接口的都能用（DeepSeek、Gemini、智谱等），
  续写 / 润色 / 抓虫。**AI 永远不会自己跑**，你不点它就不动
- **账号**：走系统浏览器登录（OIDC + PKCE），**软件从头到尾看不见你的密码**

出包前修掉的几件：

- **吞字**：切屏回来会拿磁盘那版覆盖你没保存的内容。
  现在两版对不上时让你自己挑，不再替你决定。
- 番茄钟一关侧边栏就归零
- 换字体没反应（编辑器被 monospace 压住）
- 楷体的标点比字重，看着像从别的字体里掉进来的

---

## 致谢

**感谢 zhangls 提供的 bug 报告。**

0.3「咕咕之禁城」的每一条改动，**都来自 zhangls 报上来的问题** ——
从「侧边栏钉起退出后会收起」到「路线图设置有一定延迟」，
从「变量增加操作不清楚」到「遇到相同的事件名称会回绕回去」。

其中两条是他在这一版做到一半时报的，当场就修进去了：
「退回书架时不该弹『回去接着写』」和「`@` 没有和 `#` 同款的行前悬挂」。

这些是只有真的拿它写东西的人才会碰到的问题。
写代码的人自己试不出来 —— 因为他知道该怎么用，而这恰恰是最不该有的前提。

尤其要谢的是那几条**说清了「为什么难受」而不只是「哪儿坏了」**的反馈。
比如「大部分有分歧但之后故事线依旧一样的分支设置十分麻烦」——
这句话直接指向了一个语法上的缺口，也就有了 `<- 甲、乙` 这个合并写法；
比如「防止事件名本身就是【结束】的提前结束分支」——
这不是随便一句抱怨，那是把 bug 的成因和边界一起说清楚了。

**bug 报告是这个软件唯一的信息来源。** 请继续报。
