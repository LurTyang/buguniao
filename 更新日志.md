# 更新日志

不咕鸟每一版改了什么。**最新的在最上面。**

用得上的话：报 bug 时带上版本号（设置 → 账号最底下那行），
我才知道你手上跑的是哪一份 —— 代码早改了而你还在用上一个 exe，
这种事发生过。

---

## 0.3.0 「咕咕之禁城」 · 2026-09-01

这一版**整版都是修 bug**，没加新的大功能。
十二条问题全部来自 **zhangls** 的反馈 —— 详见文末致谢。

### 稿纸

- **侧边栏钉住之后不会再掉了。** 关掉软件再打开，钉着的还钉着。
  以前钉住只活在内存里，每次开软件都得重钉一遍。
- **主题从两套扩到五套**：纸白（默认）、**护眼**（压过灰的豆沙绿）、
  纸黄（旧纸色）、墨灰（原来的深色）、**夜间**（近黑，关灯时用）。
  设置 → 外观里换。
- **可以自己挑一份 CSS 当稿纸样式，Typora 的主题文件直接就能用。**
  设置 → 外观 → 自选样式。它们排版正文用的是 `#write`，稿纸也顶着这个 id，
  所以字体、字号、行距、标题、引用、代码块会跟着变。
  侧边栏和按钮的配色仍归上面那个主题管 —— 一份为「一整个窗口就是文档」
  写的 CSS，套到有侧边栏的界面上只会更难看。
- **按 Ctrl+S 之后右上角浮一下**，告诉你距上次**手动**保存又写了多少字，
  外加本篇总字数。自动保存不弹 —— 那是三秒一次，弹起来能把人逼疯。
- **右键菜单不会再压住你刚选中的那段字了。** 稿纸上的菜单有十几条，
  从光标处往下开正好盖住选区，右键想复制时连「我选对了没有」都核对不了。
  现在它会自己挪开：先往右（选中那段还在原地，菜单摆它旁边），
  实在不行才往下、往上。
- **打开软件时问一句「接着上次写？」**，一路回到上次那一篇、那一行。
  按回车就是回去，想先逛书架就点「留在书架」。
  **只在刚打开软件、还没进过任何一本书的时候问** ——
  写完一本退回书架时你想的是下一本，不是上一本。
  那本书被删了、改了名就当没这回事。
- **行首 `@` 现在跟 `#`、`<!--` 一样顶格了。** 原来它跟正文一样缩两格，
  一屏字里根本认不出哪行是标记。

### 游戏剧本

- **路线图不用等了。** 以前要等自动保存那三秒图才动，看着就像卡住；
  现在改一个字它就跟着变 —— 拿的是编辑器里还没存盘的那一份。
- **新写法：合并 `<- 甲、乙`。**
  「分歧一下、后面还是同一条线」是分支剧情里最常见的形状，
  以前要跑去**每一条**分支尾巴上补一行 `-> 汇合点`：五条分支五个地方，
  加一条还得记得回来补第六个，漏一个就是一条死路。
  现在在合流的那个节点上写一句就够，它自己声明谁流进来。
- **新写法：一行写多个去处 `-> 图书馆、天台、社团`。**
  自动摊成三个选项，选项文字就是节点名。
  「这个时间段去哪儿」那种自由度高的写法不用再一条条抄。
- **结局写成 `-> 【结束】`。** 方括号里写什么都算结局名。
  这样你给节点起名叫「结束」也不会把整条线提前掐掉 ——
  以前 `-> 结束` 是保留名，撞上同名节点时整条线在跳过来的那一刻就没了，
  而你只会看到「我后面写的都走不到」。
  老写法仍然认，只是书里有同名节点时以节点为准。
- **有意绕回前面的节点，写成 `-> ↩节点名`。**
  没标记就绕回去的，体检会提一句 —— 那多半是两处不小心起了同一个名字。
  **这是提示不是错**：有意的循环（回到选择菜单、重复的日常）标一下就不提了。
- **变量那一块重写了。** 现在直接告诉你：怎么写（四条常用写法，还能一键
  插进稿纸）、每个变量在哪几个节点被改过（点一下跳过去）、
  哪些变量只在条件里出现过却从来没赋过值（用到它的分支永远走不到）。
- **默认的示例文本只在第一篇出现了。** 第二篇起只给一行标题，
  不用再先删二十行「李四」。剧本也一样。

### 顺手修的

- 一个会随机变红的测试（大量磁盘写入在 Windows 上偶尔超时）。
  它是假警报，但假警报会教人「红了先重跑一遍」，而那个习惯迟早会盖掉一个真 bug。

---

## 0.2.0 「不咕之境」 · 2026-08-28

**对外统计打通了** —— 别的网站可以读到你的连胜和字数了。

- 设置 → 账号 → 对外统计：登录后认领一个**对外短名**（比如 `mingting`），
  就有了一个任何人都能读的地址：
  `https://bugu.char46.top/api/v1/u/你的短名/stats`
  贴到博客、主页、群公告里，或者让别人的网站直接 `fetch` 它。
- **会发出去的就七个数**：今天写了多少、今天的底线、本周合计、当前连胜、
  最长连胜、一起写了多少天，加上日期。书名、章节名、正文一个字都不会离开这台电脑。
- 那七个数**一直摆在按钮上面，没登录时也摆着** —— 先看清楚再决定要不要登录。
- **不认领短名就没人读得到你**。认领这个动作本身就是「我确实想公开」。
- **自动上传默认关着**，得你自己打开（打开后每半小时一次）。
- 「看看别人读到什么」走的是公开接口、不带你的令牌，跟别的网站看到的一模一样 ——
  自己看自己的特权视图会骗人。
- 「删掉服务器上的我」按一下就删干净，连短名一起放回去。
  做不到彻底删除的公开功能不该上线。

不用这个功能完全没关系，**不登录整个软件一个功能都不少**。

---

## 0.1.0 · 2026-08-27（内部，没有公开发布）

第一个跑得起来的包，只在本机试过，**没有对外放过**。
列在这儿是为了让下面那些「以前怎么怎么样」有个出处 ——
0.2「不咕之境」才是第一个发出去的版本。

- **写**：稿纸、大纲、设定集、便利贴浮出、双链 `[[ ]]`、全文检索（中文分词）、
  版本历史（每次保存留一份，改错能翻回去）
- **盯着你写**：每日目标（工作日/休息日分开设，底线和理想线两条）、
  连胜、补签、请假、番茄钟、字数统计、年度热力图、里程碑
- **长篇专用**：伏笔（埋在哪、该在哪收、过期标红）、关联、灵感箱
- **剧本 / 游戏剧本**：剧本排版（只改显示，不动文件一个字节）、
  谁的戏多、场次重排、分支节点图、体检（断头路 / 孤儿 / 死路 / 走不到的分支）、
  从任意节点试玩、导出 Ren'Py / ink 骨架
- **AI**：兼容 OpenAI 接口的都能用（DeepSeek、Gemini、智谱等），
  续写 / 润色 / 抓虫。**AI 永远不会自己跑**，你不点它就不动
- **账号**：走系统浏览器登录（OIDC + PKCE），**软件从头到尾看不见你的密码**

出包前修掉的几件：

- **吞字**：切屏回来会拿磁盘那版覆盖你没保存的内容。
  现在两版对不上时让你自己挑，不再替你决定。
- 番茄钟一关侧边栏就归零
- 换字体没反应（编辑器被 monospace 压住）
- 楷体的标点比字重，看着像从别的字体里掉进来的

---

## 致谢

**感谢 zhangls 提供的 bug 报告。**

0.3「咕咕之禁城」的每一条改动，**都来自 zhangls 报上来的问题** ——
从「侧边栏钉起退出后会收起」到「路线图设置有一定延迟」，
从「变量增加操作不清楚」到「遇到相同的事件名称会回绕回去」。

其中两条是他在这一版做到一半时报的，当场就修进去了：
「退回书架时不该弹『回去接着写』」和「`@` 没有和 `#` 同款的行前悬挂」。

这些是只有真的拿它写东西的人才会碰到的问题。
写代码的人自己试不出来 —— 因为他知道该怎么用，而这恰恰是最不该有的前提。

尤其要谢的是那几条**说清了「为什么难受」而不只是「哪儿坏了」**的反馈。
比如「大部分有分歧但之后故事线依旧一样的分支设置十分麻烦」——
这句话直接指向了一个语法上的缺口，也就有了 `<- 甲、乙` 这个合并写法；
比如「防止事件名本身就是【结束】的提前结束分支」——
这不是随便一句抱怨，那是把 bug 的成因和边界一起说清楚了。

**bug 报告是这个软件唯一的信息来源。** 请继续报。
