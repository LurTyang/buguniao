{
  "name": "@bugu/core",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "description": "Pure logic shared by desktop and mobile. No fs, no window, no Electron, no Capacitor.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "diff": "^7.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/diff": "^7.0.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
