# 不咕鸟 · 开发记录

> 需求与设计在 [更新文档/](更新文档/README.md)，这里只记**做了什么**。
> 每完成一段工作追加一行。

## 当前状态速览

| 项 | 状态 |
|---|---|
| 阶段 | **一期 + 二期（桌面端）完成**：M0–M4、M6–M12 全做完；只剩 M5 移动端与 iOS |
| 测试 | **1187 通过**（core 918 + desktop 269）+ 端到端冒烟 **197 步** |
| 类型检查 | 干净（两个包） |
| 安装包 | ✅ 安装版 + 免安装版；**打包版冒烟 159 步全过** |
| 待作者验的 | AI（要真 API Key，deepseek/gemini 各跑一遍）、CodeMirror 的中文输入法、安卓 WebDAV |

一句话概括当前进度：**桌面端该有的都有了，可以当主力写作工具用。**

### 能用的功能

**写字**：建作品 / 建卷 / 建章节 / 自动保存（3 秒）/ 重命名 / 拖拽排序 /
换卷 / **回收站面板（放回原处、清空）** / txt 导入分章 / 导出 txt·Word·分章文件。

**找东西**：中文全文检索（FTS5 trigram）/ <kbd>Ctrl+P</kbd> 跳章节 /
关联面板（正向 + 反向双链，坏链单独标出）。

**不咕**：今日字数与签到 / 日周月曲线 / 年度热力图 / 连续天数 / 补签 / 番茄钟 /
**码字计划**（底线+理想线、工作日与休息日分开、请假留痕、里程碑与截止日）。

**剧本**：认场景/动作/台词、剧本排版（只改显示）、谁的戏多、场次跳转。

**看全局**：分支节点图（从上往下分层，往回跳画虚线）/
伏笔连线（没收的拖到底，超期标红）。

**搬家**：Scrivener（.scriv 目录树 + RTF）/ 一整个文件夹的 txt·md
（按数字排序、子目录分卷、GBK 自动识别）。

**游戏剧本**：节点 / 选项 / 跳转 / 变量 / 条件，**跨文件建图**；
体检（断头路、孤儿、死路、重名、条件写坏、变量没赋过值）；
从起点走一遍出结局清单与示例路径；写作进度；带骨架的模板。

**特色**：便利贴（`@` 语法 + 拖到稿纸悬浮）/ 伏笔埋收与到期提醒 /
版本历史（回滚、并排对比、命名标记）/ 灵感箱 /
AI 助手（**默认走 OpenAI 兼容端点**，deepseek、gemini、本地模型都能接，
端点地址和单价自己填；Anthropic 作为可选项，它带联网搜索）。

**同步**：坚果云冲突副本的**并排对比与处理**（用左边 / 用右边 / 两份都留，
三条路都不硬删）/ 窗口重新获得焦点时自动捡回别处的改动。

**上手**：首次启动的六条指引 + <kbd>F1</kbd> 快捷键表。

## 已完成的模块

全部在 `app/packages/core/`，**零平台依赖**（不引用 fs / window / Electron / Capacitor）。

| 模块 | 职责 | 测试数 |
|---|---|---|
| `types/` | 全项目共享类型 | — |
| `sticky/` | 便利贴 `@` 语法解析、卡片标题提取 | 64 |
| `frontmatter/` | front-matter 与 book.yaml 读写、id 生成 | 38 |
| `wordcount/` | 双口径字数统计、Markdown 清洗 | 29 |
| `foreshadow/` | 伏笔锚点解析、多设备 jsonl 合并、清单与到期提醒 | 54 |
| `history/` | 版本历史 diff/patch、时间桶、快照、清理 | 64 |
| `ordering/` | 四位间隔编号、拖拽排序、整段重排 | 37 |
| `importer/` | 中文数字解析、txt 自动分章 | 47 |
| `storage/` | StorageBackend 接口、路径工具、目录约定 | — |
| `storage/memory` | 内存版后端，供测试与移动端过渡期使用 | — |
| `repository/` | 书架扫描、目录树、文档读写、分类模板、回收站 | 127 |
| `stats/` | 会话切分、日/周/月聚合、热力图、连续天数、签到补签 | 75 |
| `exporter/` | 纯文本/分章/Word 结构化导出，标记清理与缩进 | 47 |
| `conflict/` | **坚果云冲突副本**：配回正本、左右对齐、一句话说清差异 | 28 |
| `fuzzy/` | 快速跳转的模糊匹配（连续子串优先，退而求其次是子序列） | 14 |
| `aiparse/` | 把 AI 回答拆成结构：三个续写方向、润色逐字对比、抓虫清单 | 30 |
| `script/` | 剧本：认行、按角色统计台词量、场次清单、排版导出 | 43 |
| `gamescript/` | 游戏剧本：节点/选项/跳转/变量/条件、跨文件建图、体检、走一遍 | 66 |
| `graphlayout/` | 分支节点图布局：分层、层内排序减少交叉、往回跳标虚线 | 29 |
| `foreshadowlines/` | 伏笔连线布局：区间、泳道分配、超期判定 | 23 |
| `foreign/` | 从别的软件搬家：Scrivener 的 scrivx 与 RTF、文件夹批量 | 32 |
| `plan/` | 码字计划：目标线与变更历史、工作日/休息日、补签、请假、连续天数 | 37 |
| `milestone/` | 里程碑：进度、截止日、每天要写多少、按速度预计会晚几天 | 31 |

`app/packages/desktop/`（可用 Node API）：

| 模块 | 职责 | 测试数 |
|---|---|---|
| `storage/local-fs` | 真实文件系统后端：原子写入、路径越界拦截、BOM 处理 | 40 |
| `main/ai-provider` | 服务商接口、预设、本地 token 粗估 | 44（与下面两个合计） |
| `main/ai-openai` | OpenAI 兼容端点（**默认**）：流式、用量、各家字段差异 | 同上 |
| `main/ai-anthropic` | Anthropic：缓存前缀、服务端联网搜索、精确数 token | 同上 |
| `main/workspace` | 保存管线、目录管理、伏笔、历史、灵感箱、冲突处理 | 41 |
| `main/index-db` | 索引层：FTS5 全文检索、双链正反查、增量同步 | 53 |
| `main/transfer` | 编码检测（BOM→严格 UTF-8→GBK）、txt/docx/分章导出 | 23 |
| `main/ai` | Key 分家加密、上下文分层、四项能力、费用统计、老配置迁移 | 见上 |
| `main/smoke` | 端到端冒烟：真起 Electron 跑完整流程 | **125 步** |
| `renderer/` | 书架、写作页、编辑器、侧边栏、17 个面板与浮层 | 冒烟覆盖 |

## 工作过程

| 内容 | 日期 | 耗时 |
|---|---|---|
| 需求访谈四轮，产出 8 份开发文档（产品定义 / 技术架构 / 数据格式规范 / 界面设计 / 功能详述 / 路线图 / 待决事项） | 2026-08-25 | 约 1 小时 |
| 搭 pnpm workspace 骨架（core / ui / desktop / mobile），接入 vitest + TypeScript strict | 2026-08-25 | 约 15 分钟 |
| 实现便利贴 `@` 语法解析器 + 51 测试；发现并修正规范中规则 B 的歧义 | 2026-08-25 | 约 30 分钟 |
| 实现 front-matter / book.yaml 读写 + 36 测试；重点保证未识别字段往返不丢 | 2026-08-25 | 约 25 分钟 |
| 实现双口径字数统计 + 29 测试；确保标伏笔不会让字数凭空上涨 | 2026-08-25 | 约 20 分钟 |
| 实现伏笔追踪 + 54 测试；正文注释锚点 + 仅追加 jsonl 合并 | 2026-08-25 | 约 35 分钟 |
| 实现版本历史 + 58 测试；采用作者修正的固定 30 秒时间桶 | 2026-08-25 | 约 40 分钟 |
| 实现四位间隔编号排序 + 37 测试 | 2026-08-25 | 约 20 分钟 |
| 实现 txt 导入分章 + 47 测试；用最长递增子序列剔除正文噪音 | 2026-08-25 | 约 30 分钟 |
| 实现存储抽象与路径工具；core 类型检查转干净 | 2026-08-25 | 约 15 分钟 |
| 实现内存版存储后端 + 作品仓库（书架/目录树/模板）+ 53 测试 | 2026-08-25 | 约 40 分钟 |
| 实现本地文件系统后端 + 40 测试；修复并发临时文件撞名与 Windows rename 偶发 EPERM | 2026-08-25 | 约 35 分钟 |
| 打通 workspace 级 `pnpm check`（类型检查 + 全部测试） | 2026-08-25 | 约 10 分钟 |
| 实现统计聚合 + 52 测试；写作日按凌晨 4 点分界，热力图用分位数分级 | 2026-08-25 | 约 30 分钟 |
| 实现导出模块 + 47 测试；标记清理、首行缩进、分章导出、Word 结构化输出 | 2026-08-25 | 约 30 分钟 |
| 按第三轮决议改写 @ 规则（恰好一个）、写作日归属（按会话）、签到补签 | 2026-08-25 | 约 50 分钟 |
| 搭 Electron + React + CodeMirror 外壳；书架页、写作页、侧边栏 | 2026-08-25 | 约 70 分钟 |
| 修打包配置三处错（主进程 CJS、electron 外部化、preload 扩展名） | 2026-08-25 | 约 25 分钟 |
| 目录管理：重命名、回收站、新建卷、拖拽排序、换卷、设定分类 + 32 测试 | 2026-08-25 | 约 55 分钟 |
| 端到端冒烟升级为完整流程测试 | 2026-08-25 | 约 30 分钟 |
| 索引层 + 全文检索 + 检索面板 + 46 测试；排掉 SQLite 原生模块风险 | 2026-08-25 | 约 65 分钟 |
| 接入 electron-builder，产出安装版与免安装 exe；画了占位图标 | 2026-08-25 | 约 35 分钟 |
| 按首次试用反馈改 8 处：右键菜单失效、触发区、图标简约化、设置面板、书架右键、中文菜单等 | 2026-08-25 | 约 90 分钟 |
| 便利贴悬浮、伏笔面板、版本历史面板、番茄钟、统计浮层、导入导出界面、AI 面板 | 2026-08-25 | 约 3 小时 |
| **灵感箱**：面板 + 归入（追加到目标末尾，碎片进回收站）+ 12 测试 + 10 步冒烟 | 2026-08-25 | 约 40 分钟 |
| **坚果云冲突并排对比**：`core/conflict` 28 测试 + 处理三选一 + 13 测试 + 14 步冒烟 | 2026-08-25 | 约 70 分钟 |
| **帮助**：首次启动六条指引 + F1 快捷键表 | 2026-08-25 | 约 25 分钟 |
| **Ctrl+P 快速跳转**（`core/fuzzy` 14 测试）+ **关联面板**（正反双链，7 测试） | 2026-08-25 | 约 50 分钟 |
| 焦点重扫：窗口回来时捡回别处改动，正文脏着时只提醒不覆盖 | 2026-08-25 | 约 20 分钟 |
| 一期收尾：路线图对齐实际、重新打包、打包版冒烟 125 步全过 | 2026-08-25 | 约 30 分钟 |
| 修字体两处毛病：编辑器被 CodeMirror 的 monospace 压住、楷体标点比字重（D-20~D-23）+ 15 测试 | 2026-08-25 | 约 60 分钟 |
| **OpenAI 兼容端点（M9）**：服务商抽象、默认改成 openai、端点/模型/单价都自己填、老配置迁移 + 44 测试（含假服务器真跑一遍） | 2026-08-25 | 约 100 分钟 |
| 按试用反馈改三处：侧边栏可拖宽、模型改下拉框且默认给便宜的、去掉服务商介绍小字（D-27~D-29） | 2026-08-25 | 约 45 分钟 |
| 接代理（gemini 连不上的真正原因）、单价改三档、加币种与空闲时段半价、接智谱端点、设置区可收起、思考计时（D-30~D-37）+ 39 测试 | 2026-08-25 | 约 110 分钟 |
| DeepSeek 单价按官方价目表逐条核对；**补上回收站界面**（D-38，之前只有后端，界面上根本捞不回来）+ 8 步冒烟 | 2026-08-25 | 约 40 分钟 |
| AI 续写/润色/抓虫改成结构化输出（三个方向分别采纳、逐字对比、可跳转清单）+ 停止按钮 + `core/aiparse` 30 测试 | 2026-08-26 | 约 90 分钟 |
| **剧本模式第一版（M10）**：`core/script` 43 测试、编辑器剧本排版、剧本面板（谁的戏多 / 场次跳转） | 2026-08-26 | 约 80 分钟 |
| **游戏剧本第一版（M11）**：节点/选项/跳转/变量/条件、跨文件建图、六种体检、走一遍出结局与路径、写作进度、模板 + 66 测试 + 9 步冒烟 | 2026-08-26 | 约 120 分钟 |
| **二期（M12）**：分支节点图（29 测试）、伏笔连线（23 测试）、Scrivener 与文件夹导入（32 测试）；顺带修了「刚建的章节从伏笔章节轴上消失」 | 2026-08-26 | 约 150 分钟 |
| 修封面碎图标（CSP 拦了 data: URL） | 2026-08-26 | 约 20 分钟 |
| **码字计划（M13）**：底线+理想线、工作日/休息日、目标变更历史、补签按倍数、请假留痕、里程碑（37+31 测试）| 2026-08-26 | 约 140 分钟 |
| 稿纸右键菜单：编辑命令 + 插入各种语法符号（D-42） | 2026-08-26 | 约 40 分钟 |
| **用户信息与鼓励（M14）**：书架左侧用户栏、写作页连胜、总设置浮层、启动问候 | 2026-08-26 | 约 90 分钟 |

## 开发中发现的设计问题

这些是**写代码时才暴露、设计阶段没预料到**的问题。全部已修正，详见
[07-待决事项 · 决议记录](更新文档/07-待决事项.md#开发中产生的规范修正)。

1. **`@` 语法规则 B 有歧义**（D-01，⚠️ 待作者复核）
   `@十七岁@，实为三百余岁` 会先命中「行首整行浮出」，而作者本意是行内成对。
   给规则 B 加了「@ 个数为奇数」条件后，四条规则互斥且完备。

2. **伏笔 id 示例不合自己的规范**（D-02）
   文档里写「6 位 base36」，举的例子 `f7k2p9` 只有 5 位。

3. **unified diff 对中文长段落几乎无效**（D-03）
   diff 是按行比对的，中文一个自然段就是一行。改一个字要把整段重写两遍，
   段落够长时增量比全量还费。加了保险阀：增量不比全文小就改存快照。

4. **jsdiff 对损坏的补丁悄悄返回原内容**（D-04）
   既不抛异常也不返回 false。作者以为回到了第 100 版，拿到的其实是第 99 版，
   毫无提示 —— 这是最坏的一种错误。改用记录里已有的字数字段做完整性校验。

5. **原子写入的临时文件会撞名**（D-05）
   临时文件名原本用 `pid + 毫秒`。但自动保存、历史写入、统计写入完全可能
   落在同一毫秒里，撞名会让第二次写入直接失败 —— 作者会丢一次保存。
   改成 `pid + 进程内单调计数器 + 随机后缀`。
   这是「并发写同一文件」那条测试抓出来的。

6. **右键菜单里每一项都点不动**（D-10，作者试用时发现）
   关菜单用的是 `mousedown` 的**捕获阶段**监听 window。捕获是从 window
   往下走的，于是点菜单项时 window 先收到事件把菜单关掉、组件卸载，
   `click` 根本没机会触发。在容器上 `stopPropagation` 也救不了，
   因为捕获比它更早。重命名、删除、换卷、编辑模板全中招 ——
   作者反馈的「编辑模板没反应」和「删设定集没反应」是同一个 bug。

7. **serializeDoc 往返不是恒等的**（D-07）
   序列化时会在正文前偷偷补一个换行，让文件好看点。代价是
   `parseDoc(serializeDoc(x)) !== x` —— 作者第一次保存后，
   编辑器顶上凭空多出一个空行。
   改成严格恒等，排版好看交给新建文档时处理。
   **这是端到端冒烟抓出来的**，单元测试全绿也没发现。

8. **归入灵感 / 覆盖正文时会多出一个空行**（D-14）
   目标文档是空的时候，`空 + 

 + 内容` 会在正文顶上留一个空行 ——
   和 D-07 是同一类毛病，作者打开一看以为自己按错了键。
   顺带给 `writeNewDoc` 加了 `keepBodyAsIs`：新建空文档补空行是排版好看，
   但从冲突副本、灵感碎片搬过来的正文是有内容的，那一行会直接扎在正文顶上。

9. **「两份都留」会让冲突副本永远处理不完**（D-16）
   冲突副本的标题是从文件名来的，直接拼「（另一版）」会把 `(冲突文件 …)`
   一起带过去 —— 新文件下次扫描**又是**冲突副本。作者点一次多一个。

11. **换字体一直没反应，正文永远是微软雅黑**（D-20，作者试用时发现）
    CodeMirror 的 baseTheme 里有一条 `.cm-scroller { font-family: monospace }`，
    和我们写的 `.cm-scroller { font-family: inherit }` **权重完全一样** ——
    谁排在后面谁赢。打包之后 CodeMirror 那条排在了后面。
    于是正文是 monospace；而 monospace 里没有汉字，Chrome 只好回落到
    系统默认中文字体，看上去就是微软雅黑。
    **顺带还解释了作者说的「问号有点奇怪」**：？ 和汉字回落到的不是同一个字体。
    加一级选择器（`.cm-editor .cm-scroller`）权重就压过去了，跟先后顺序无关。
    这个 bug 单元测试完全测不到 —— 是把真窗口截图出来比对才看见的。

12. **楷体自带的标点比它的汉字重**（D-21）
    修完 D-20 之后剩下的那一点别扭。Windows 自带楷体的 `？！「」——`
    是另一套粗笔画，摆在一行细笔画楷字里很跳。
    用 `@font-face` + `unicode-range` 把 CJK 标点单独指到仿宋，
    范围只圈标点，一个汉字都不碰；没装仿宋就自动退回楷体本身。

13. **跑一次冒烟会把作者真实的设置覆盖掉**（D-24，加 AI 测试时发现的）
    冒烟把作品根目录指到临时文件夹，但**配置目录用的还是真实的那个**。
    于是每跑一次冒烟，作者的作品根目录设置就被改掉一次，AI 配置同理。
    而且上一次冒烟留下的配置还会让这一次的断言时灵时不灵 ——
    我就是因为上一轮留下的旧版 AI 配置，看到一条断言莫名其妙地红了才发现的。
    改成冒烟模式下连 `userData` 一起指到临时目录。

10. **版本历史兜不住「用副本覆盖正本」**（D-17，单元测试抓出来的）
    原打算：覆盖前那一版由版本历史保管。但历史按 30 秒时间桶合并，
    刚保存完就处理冲突的话，被换掉的那一版会并进同一条记录里，
    **根本不留痕迹**。改成先把正本整个移进回收站再覆盖 ——
    回收站里躺着一个完整文件，比一条可能被合并掉的历史记录踏实得多。

7. **Windows 上 rename 会偶发 EPERM**（D-06）
   杀毒软件、Windows 搜索索引器、坚果云客户端都可能在那一瞬间持有目标文件句柄。
   原子写入依赖 rename，一次自动保存因此静默失败是不可接受的。
   加了退避重试（10/20/40/80/200ms），总等待约 350ms，远低于人能察觉的程度。
   **这一条对本项目尤其重要**：作品文件夹就放在坚果云同步目录里，
   坚果云客户端随时可能在读我们刚写的文件。

## 待验证的技术风险

按优先级，见 [07-待决事项 §C](更新文档/07-待决事项.md#c--已知的技术风险)。
其中 **C1（安卓 WebDAV 的 CORS/证书）** 是唯一可能推翻现有同步方案的风险，
建议在 M5 写任何界面之前先做最小连通验证。

### 需要你亲手验的三件事

我这边跑不了，只能你来：

1. **中文输入法**。CodeMirror 6 在 Windows 输入法下的候选框位置、
   连续输入时的光标跳动，只有真人打字才知道好不好用。写一章试试。
2. **AI**。要真实 API Key 才发得出请求。验收看三条：
   问「主角现在什么状态」能基于正文答；连问 10 次从第 2 次起有缓存命中；
   全程没有任何 AI 输出被自动写进正文。
3. **两台电脑之间的坚果云同步**。在 A 电脑写一段、等同步、在 B 电脑打开，
   看能不能正常接着写。故意制造一次冲突（两边离线各改同一章），
   看顶上的黄条和并排对比是不是好用。

## 怎么验证现在这些东西是好的

```bash
cd 不咕鸟/app && pnpm check
```

它会跑两个包的类型检查和全部 1187 个测试。

再跑一次端到端（会真的启动 Electron，在临时目录里走完整套流程）：

```bash
cd 不咕鸟/app/packages/desktop && pnpm smoke
```

结果写在 `out/smoke-result.json`。两个都绿就说明没坏。

想自己看看（从源码跑，改代码即时生效）：

```bash
cd 不咕鸟/app/packages/desktop && pnpm dev
```

想要能双击的 exe：

```bash
cd 不咕鸟/app/packages/desktop && pnpm dist
```

产物在 `不咕鸟/app/packages/desktop/release/`：
- `不咕鸟-免安装-0.1.0.exe` —— 直接双击就能跑，不装
- `不咕鸟-安装版-0.1.0.exe` —— 正常安装，带开始菜单和桌面快捷方式

打包产物**不进 git**（一百多兆），需要时现打。
验证打包版是不是好的：

```powershell
$env:BUGU_SMOKE_OUT="$PWD\release\smoke.json"; & ".\release\win-unpacked\不咕鸟.exe" --smoke
```

## 几个自作主张的细节，请你过目

写代码时遇到规范没覆盖、但必须当场定下来的地方。都往「对作者更体贴」的方向定的，
觉得不对随时改：

1. **凌晨 4 点才算新的一天。** 写手常写到凌晨两三点，那显然还是「今天」的产出。
   按 0 点分界的话，热力图上会莫名其妙地出现「连续天数中断」。
2. **今天还没写时，连续天数从昨天起算。** 今天才刚开始，不该因为还没动笔就清零。
   但**昨天也没写就是真断了**，不粉饰。
3. **净增字数为负或为零的那天，不算「写了」。** 删删改改的日子如果也算连续，
   那这个数字就是自欺欺人。
4. **热力图的颜色深浅按分位数切，不按固定字数。** 日更三千和日更三万的作者，
   用同一套固定阈值会让其中一个的图整片死绿或整片死红。
5. **导出到 Word 时不塞全角空格**，改用段落样式缩进 —— 这样编辑在 Word 里
   能直接调格式，而不是对着一堆空格干瞪眼。导出成 txt 时才塞空格。
6. **冲突副本的三条路都不硬删。** 「用右边」时被换掉的正本整个进回收站，
   「两份都留」时副本变成 `xxx（另一版）`。同步冲突里被丢掉的那一版，
   往往是你熬夜写的那一版 —— 所以界面上**刻意没有「自动合并」按钮**。
7. **外部改动靠「窗口回来时重扫」，不靠文件监听。** 坚果云同步时是一个文件
   一个文件地落盘，监听一次能打出上百个事件；而你真正会察觉到的时机只有一个，
   就是你回到这台电脑前面。正文脏着的时候只提醒、绝不覆盖 —— 那是你没保存的字。
8. **快速跳转只搜标题，不搜正文。** 搜正文有 <kbd>Ctrl+Shift+F</kbd>，
   两个功能混在一起的结果是两个都不好用。
9. **坏掉的双链要显示出来。** 名字写错、卡片还没建的 `[[链接]]` 在关联面板里
   单独标出来并给一个「建一张」的按钮。藏起来的话，你会以为链接是好的。
10. **没做自动更新。** electron-updater 要一个放更新包的服务器。
    自己一个人用的软件，重新下一个 exe 比架服务器省事得多。真要发给别人时再加。
11. **AI 的默认服务商是 OpenAI 兼容端点，不是 Claude。** 作者用 deepseek
    和 gemini，默认摆一个他用不上的服务商，等于这块功能开箱就是坏的。
12. **AI 的单价让作者自己填。** 各家随时调价，软件写死的数字迟早骗人。
    填 0 就老实说「算不出钱」，只报 token 数 —— 编一个金额出来比不报更糟。
13. **估出来的 token 数要标明是不是粗估的。** 只有 Anthropic 有数 token 的
    接口；别家是本地按字数估的，不说清楚的话作者会以为这个数很准。
14. **AI 请求走代理，默认跟随系统环境变量。** Node 自带的 fetch 不认
    `HTTPS_PROXY`，所以哪怕你开着代理它也不会用 —— 这就是 gemini 连不上的原因。
    设置里那一行会告诉你**现在实际走的是什么**，旁边还有「测试连接」。
15. **单价分三档填，因为缓存命中与未命中差三十倍**（DeepSeek 是 0.10 对 3.00 元）。
    拿一个固定比例糊会把花销高估三倍。
16. **空闲时段半价按北京时间算，不按本机时区。** 你可能人在别处，
    但服务商的时段是按北京时间定的。你写到凌晨那一段正好是半价。
17. **模型默认给便宜的那一款。** 每家的下拉框里便宜的排在最前，第一款就是默认。
    默认给个旗舰，等于替你做了一个你没同意的花钱决定。换模型时单价跟着换。
18. **侧边栏宽度按面板记，不按左右记。** 你把哪个面板拖宽，是因为那个面板
    内容多，跟它当时摆在左边还是右边没关系 —— 所以左右互换之后，宽度跟着面板走。
19. **剧本模式不发明新符号。** 场景用 `#`、动作用 `（）`、台词用「角色名：台词」——
    都是中文剧本本来就在用的写法。**一份剧本文件用别的软件打开、直接打印出来，
    仍然是一份剧本**，这比多几个花哨的语法重要得多。
20. **剧本排版只改显示，文件一个字节都不动。** 界面上明写这句话，
    并且有测试盯着：开了排版再读一遍文件，必须和之前完全一样。
21. **「谁的戏多」默认按台词字数排，不按出场顺序。** 剧本作者最想知道的
    就是谁被写多了、谁被写没了。
22. **游戏剧本的体检比排版重要得多。** 断头路、孤儿节点、永远走不到的分支，
    这些在几十个文件里靠人眼是翻不出来的 —— 写完一整条线才发现某个结局
    根本拿不到，那是几万字白写。所以「体检」那一页是这个面板真正的价值。
23. **模拟走不完时要在界面上明说「不是全的」。** 带变量的剧本状态空间会爆炸，
    有上限。作者拿这个数字判断「这个结局拿不到」，糊弄他会出事。
24. **没赋过值的变量拿数字比时当 0。** `{好感度<1}` 在一次都没加过好感度时
    必须成立 —— 不然「冷淡线」会被判成永远走不到，而它恰恰是默认路线。
25. **分支节点图的布局算法放在 core 里，有测试。** 「哪个节点摆在哪」是纯计算，
    而且很容易悄悄算错 —— 环状跳转把层数算成无穷、孤儿节点被摆到画布外面。
    这些用眼睛看图是发现不了的。
26. **没收的伏笔画成虚线拖到最后一章。** 拖得越长越扎眼，这正是目的 ——
    埋了八十章还没收的那几条，本来就该一眼看见。
27. **青茉、码字精灵没有样本文件就不猜格式。** 猜错等于把你几百万字导成乱码，
    而且看着还像成功了。含糊其辞地「支持」一个没验过的格式，
    比明说不支持糟糕得多。

### 这一轮踩到的两个坑，值得记一笔

- **源码里混进了看不见的控制字符。** 用脚本改文件时 `` 被当成转义，
  在正则里留下一个真正的退格字符（0x08），于是那条正则**永远匹配不上**，
  Scrivener 只能导出第一章。全仓扫了一遍，另有一处 NUL，也改成了显式转义。
  教训：正则和字符串里的反斜杠，别经过多层脚本转义。
- **章节轴上的章节会凭空消失。** 伏笔的章节顺序是从索引里取的，
  而刚建还没保存过的章节不在索引里 —— 它会从连线图上消失，
  挂在它上面的伏笔也跟着失去位置。是画连线图的时候才看出来的。
28. **改了每日目标，以前的日子仍按当时的目标算。** 目标存的是变更历史，
    不是一个数。没有这一条的话，你调一次目标整张热力图和连续天数当场全变 ——
    那是在骗自己，而这软件叫「不咕」。
29. **补签线是底线的倍数，不是绝对字数。** 目标 1000 和目标 8000 的人，
    「今天多写了一倍」含义一样，绝对值却差八倍。
30. **请假不算达标，只是不断链。** 热力图上画中性色，一眼看得出那天没写。
    面板上还会写明这段连续里有几天是补签、几天是请假撑着的。
31. **达标按全库合计算。** 目标是「人」的属性不是「书」的属性 ——
    「每天写 8000 字」不分在写哪本。
32. **「会晚几天」只在会晚的时候说。** 早到不邀功，提醒的价值在于坏消息。
33. **连胜要显眼，断了要平静。** 多邻国断了会弹一堆挽留，那是在卖付费补签。
    这儿断了就是安静地重新计数，旁边留一行「上次连了 N 天」当参照 ——
    那是「上次到过哪儿」，不是「你搞砸了」。
34. **不发明第二套护级卡。** 补签额度已经有了就用它，而且要写明连续里
    有几天是撑出来的 —— 不然这个数字就是在骗自己。
35. **徽章只在 7/30/100/365 天给，启动问候一次只弹一次。**
    天天弹恭喜会麻木，弹第二次就从鼓励变成打扰。
36. **「一起写了 N 天」不叫「注册天数」。** 这软件没有账号，也不存邮箱密码。
    按第一条码字记录算比按第一次启动算实在。
37. **本周达标的分母要刨掉请假日。** 出差三天还显示 4/7，
    会让作者觉得自己在偷懒，而他并没有。
38. **块条件不认全，比不做块条件更糟。** `$若 …` 里那五条选项如果只当成普通
    选项收下，体检会说它们「走得到」，结局模拟会把不该出现的结局列出来。
    作者拿这个判断「这条线通不通」，糊弄他就是让他白写几万字。
    所以出口和赋值都挂上继承来的条件链，模拟时逐条求值。
39. **试玩必须能从中段开始，还得能假设变量。** 写到第八章想试
    「从这儿往后还能到哪些结局」，每次从头走一遍是没法用的；
    而且不让他假设「已经拿到钥匙了」，条件分支一条都进不去，
    试出来的结果是假的。
40. **Ren'Py 用中文名，ink 不能。** Ren'Py 是 Python 3，中文标识符合法，
    节点名原样留着最好认。ink 的 knot 名只认字母数字下划线，
    中文剥掉会剩空字符串，导出 `VAR  = 0` —— 既不报错也不能用。
    所以 ink 那份全换成 `n1`/`v1`，**并且在文件开头留一张对照表**：
    不留的话作者打开一看全是 n1 n2，根本认不出哪个是哪个。
41. **「这是骨架不是能跑的游戏」要写在按钮旁边。** 只写在导出文件的头部没用 ——
    他先看到的是按钮。让他以为导出来就能跑，他会拿去开工程，
    然后发现立绘、音乐、转场、界面少了一半。
42. **场次重排用 ↑↓ 不用拖拽。** 这个操作**直接改正文**（场次顺序就是正文
    里的顺序，没有第二份顺序数据），点错一下比拖错一下好收拾；
    而且它走完整的保存管线，版本历史里留得下这一步，挪错了能回滚。
43. **「谁好久没出声了」只看总台词量是看不出来的。** 一个角色可以总字数很高，
    却全挤在前三场。群像戏最怕这个，所以要摊到场上算相邻两次开口的间隔。
44. **导出套剧本排版只对看着像剧本的章节生效。** 一本小说里夹着两章剧本很常见，
    无条件套上去会把正常章节排得莫名其妙。

### 这一轮的工具账

看图（`--uishot`：开真窗口→点到某个状态→截图）这次差点看走眼：
**窗口没被盯着的时候 Chromium 会省着画，`capturePage()` 拿到的是上一帧。**
DOM 里明明已经切到「试玩」页，截出来的图还停在「图」页 ——
差一点就当成「点击没生效」去改代码了。解法是截图前先逼它重绘，
丢掉第一张、取第二张。
45. **剧本要认人，不能靠猜。** `角色名：台词` 靠正则卡得再死，也挡不掉
    「时间：三年后」。而设定集里本来就写着这本书有哪些人 ——
    把那份名单读进来，「李四」就是确凿的，「时间」就不是。
    有了确凿的名单才敢把角色名单独排一行：拆错了就是把一句叙述
    从中间劈成两半。**不在名单里的一律不拆行。**
46. **拆行靠 CSS，不靠改文件。** 一个 `display:block` 的 span，后面的文字
    自然落到下一行 —— 一条源码行显示成两行，而文件里还是那一行。
    关掉排版就回去了，第一条铁律没破。
47. **光标停在哪一行，那一行就不藏字符。** 冒号在两行式排版里是多余的，
    但正在改的那行如果字符会凭空消失，退格删到哪儿就全靠猜了。
48. **「这些名字不在人物卡里」抓的是写错的人名。**「李西」和「李四」
    在统计表里是两个人，而作者盯着那张表根本看不出来 —— 两行长得几乎一样。
    没勾人物分类时一条都不报：那时候每个名字都「不在卡里」，全是噪音。
49. **统计按写在纸上的名字算，不偷偷把别名并进正名。** 认得「小李」是李四
    是为了排版，不是为了替作者改稿。显示什么就统计什么，
    偷偷归并会让他对不上数。
50. **`castFrom: []` 和「没有 castFrom」是两件事。** 前者是作者选了
    「一个分类都不算人物」，后者是他还没选过（那时候按分类名猜）。
    空数组不写进 book.yaml，下次打开又会被猜成认人 —— 这个 bug 是冒烟抓到的。
51. **新建时就问「写的是哪种」。** 写完三章才发现「原来还有剧本模式」，
    那时候格式已经写歪了。剧本和游戏书顺手把「人物」分类建好并指过去 ——
    不然作者得先自己去找那个开关，认角色这件事就永远不会发生。
52. **作品类型只在书架右键改，稿纸页没有入口。** 换类型会换掉整本书的
    排版规矩，那不该是写到一半顺手点到的东西。

### 又踩了一次同一个坑

剧本排版的缩进（`.cm-sc-action` 缩两格、`.cm-sc-dialogue` 缩四格）
**从做出来那天起就没生效过**：CodeMirror 的 baseTheme 里有一条
`.cm-line { padding: 0 2px 0 6px }`，权重和单写一个类名完全一样，
而它排在后面。类名挂上了、颜色也对，就是一格都不缩 ——
看着像「剧本排版本来就没做缩进」，所以一直没人发现。

跟当初「换字体没反应，永远是微软雅黑」是同一个坑（第 12 条）。
这次是量了一遍 `getComputedStyle(el).paddingLeft` 才看出来的：
截图上「李西：等一下」和场景标题左边缘对齐，一眼看着只是「没缩进」，
量出来是 6px —— 那是 CodeMirror 的值，不是我们的。

**教训：给 CodeMirror 的行加样式，一律写 `.cm-editor .cm-line.xxx`。**
53. **账号是可选的，不是前置条件。** 第一条铁律是文件即真相 ——
    稿子在硬盘上、在同步文件夹里，不在服务器上。所以不登录整个软件
    照常用，一个功能都不少。这句话写在账号页最上面，
    不然作者一打开就以为「这软件要注册才能写」。
54. **「哪几个数可以对外」只在一个函数里说了算。** `publicStatsFrom` 是
    显式构造那七个字段，不是从 report 里 spread —— 加字段必须是有人
    专门来改这个函数，而不是别处多了个字段就自动跟着漏出去。
    冒烟里还查了服务器**实际收到**的 body 里有没有书名章节名：
    「客户端要发什么」和「服务器拿到了什么」是两件事，都得查。
55. **公开开关关着就一个数都不推。** 关着还推等于「你以为没公开，
    其实数据已经在服务器上了」—— 这种事只要发生一次，
    作者就再也不信这个软件了。
56. **非 https 一律拒绝，没有开关。** 密码是明文发过去的。
    留了开关就一定有人为了图省事打开它然后忘掉。localhost 放行是
    因为那条线出不了这台机器。
57. **退出登录是本地先清、再通知服务器。** 顺序反过来的话，
    服务器连不上时作者就退不出去 —— 而「退出」按下去就该立刻生效。
58. **密码不落盘、不进日志。** 只在注册/登录那一次调用里在内存里过一遍。
    本地存的只有令牌，safeStorage 加密，放 userData，**不进同步文件夹**
    （同步文件夹是会被网盘上传的）。跟 API Key 一个待遇。
59. **`expiresAt` 必须由服务器给。** 客户端不猜令牌什么时候过期 ——
    不给的话就只能等某个请求 401 了才发现，那时候作者正在别的事上。
60. **公开开关关着时对外接口回 404，不回 403。** 回 403 等于告诉外面
    「这个 handle 是存在的」。
61. **假服务器不是参考实现，是冒烟的对照物。** 它没有哈希、没有限流、
    数据在内存里，但它跑得通客户端的每一条路径 ——
    写真服务器时照着它对，比照着文档对准。
62. **图文说明按页面弹，不是一个总开关。** 只写小说的人不该被弹游戏剧本
    那一页；而他哪天真开了本剧本，那一页还得弹。
63. **功能栏里不该有「模式开关」。** 剧本模式、游戏剧本改成按作品类型给：
    小说书里根本不该出现「游戏剧本」这一条。边写边在侧边栏里换模式，
    等于告诉作者「格式是可以随手切的」，而它不是 —— 它是这本书的属性，
    在书架上定。
64. **码字计划搬走，里程碑留下。** 这两样看着是一码事，其实不是：
    目标是「人」的属性（每天写 8000 字，不分在写哪本），
    里程碑是「书」的属性（这一卷什么时候写完）。
    前者住总设置，后者住写作页的侧边栏。
65. **回收站按书就不用挪。** 在这本书里删掉的章节，就该在这本书里捞。
66. **灵感箱要在书架上，而且每个标签下都在。** 想到点子的时候往往
    还不知道它属于哪本书，甚至不知道要不要为它开一本书 ——
    逼着人先挑一本，那个点子多半就飞了。所以它落在全库共用的
    `_灵感箱` 里，回头再归。
67. **记灵感的框里只有一个多行输入框。** 没有标题、没有分类、
    没有「属于哪本书」。灵感是几秒钟的东西，多问一个问题它就飞了。
68. **置顶存在书自己的 book.yaml 里，不是一份全局清单。**
    那样换台电脑、把某本书单独拷走，置顶都跟着走，
    也不会留下一条指向不存在的书的记录。都置顶时仍按书名排 ——
    置顶是「常写的那几本别翻」，不是又一套要手工维护的顺序。
69. **划出来的侧边栏里，任何有状态的东西都不能把状态放在组件里。**
    番茄钟就栽在这儿：侧边栏收起来时 `Sidebar` 直接 `return null`，
    面板连同它的 state 一起卸载 —— 鼠标挪开一下回来，计时归零。
    而且不止归零：「到点了」那段逻辑也跟着不跑，专注结束不会自动进休息、
    不会响、不会计数。**等于番茄钟只在你盯着它看的时候才走。**
    修法是状态和 interval 一起挪到模块级，面板只当视图。
70. **顶栏的小胶囊也不能靠面板回调喂。** 面板一关回调就断，
    胶囊会停在最后一帧 —— 看着像「时间不走了」。让它直接订阅同一份状态。
71. **把 store 拆成一个不 import 任何东西的文件，就能在 node 里测。**
    渲染进程的 `api` 在模块顶层就 `window.bugu`，一 import 整个测试就跑不起来。
    副作用（响一声、告诉主进程）由外面注入之后，计时逻辑这块
    ——最该测的那块——16 个测试全跑得起来。
72. **写作软件吞掉一次文字，作者就再也不敢用它了。** 这比任何功能缺失
    都严重。所以「磁盘跟内存不一样」这种事，**绝不能由软件替他做决定** ——
    停下来，把两版的设备名、字数、时间摆出来，他点了才动。
73. **那个吞字 bug 的根因只有一个：`loadedBody` 保存后没更新。**
    它只在「打开文档」时赋值，自己存过之后就过期了，于是每次切回窗口
    都判定成「在别处改过」。平时覆盖的就是自己刚存的，看不出来；
    赶上后台节流的自动保存刚好在 `await readDoc` 期间落地，
    就会拿**读盘那一刻的旧内容**盖掉刚敲的那段。
    误报和吞字是同一个 bug 的两张脸。
74. **异步检查里的守卫要在 await 之后重新判一次。** 初版是
    `await readDoc` 之后才看 `saveState === 'dirty'` —— 等待期间状态早变了。
    现在改成比内容：磁盘 == 编辑器 / 磁盘 == 上次读写的，都不用管。
    比状态不如比内容，状态会在你不看的时候变。
75. **「用别处那版」之前先把手上这版另存一篇。** 顺序反过来就等于吞字。
    不问、不让他自己去复制粘贴 —— 他这会儿正慌着。
76. **CodeMirror 自带界面也要汉化。** Ctrl+F 那个查找条是
    `@codemirror/search` 画的，next / match case / regexp 全是英文。
    库里每处文案都走 `EditorState.phrases` 这个 facet，塞一张对照表就行，
    不用改库也不用自己重画一个。键名要跟原文一字不差，
    错一个字母那一项就悄悄退回英文。
77. **那个查找条不能设 `display:flex`。** 它是靠 `<br>` 分两行的
    （上面查找、下面替换），改成 flex 那个 br 就失效，替换框会挤到第一行。
78. **一组互斥选项别平铺在右键菜单上。** 书架那本书的菜单原来十一条，
    其中六条是「按 X 排版」「标为 Y」——真正常用的「打开」「重命名」
    反而得扫一遍才找得到。收成两个下级菜单，一级只剩八条。
    父项上带着当前值（`更改书籍格式（剧本）`），不展开也知道现在是什么。
79. **下级菜单要画在父项里面，不能用 portal。** 关菜单的判断是
    `ref.current.contains(e.target)`，portal 出去就不算「点在菜单里面」，
    点一下子菜单整个菜单就没了。
80. **子菜单收起要留 220ms。** 鼠标从父项斜着挪过去时会短暂地两边都不在，
    立刻收的话子菜单在半路就消失了。
81. **React 的 onMouseEnter 是 mouseover 委托实现的。** 合成一个
    `mouseenter` 事件叫不动它——第一次验证时子菜单「打不开」，
    以为是 bug，其实是测试脚本的问题。改用真的输入事件才测得准。
82. **桌面端登录不该有密码框。** 自己做登录框 = 密码要经过我们的进程，
    那我们就得对「有没有被记进日志、有没有被崩溃报告带走」负责，
    而且加两步验证、加微信登录，客户端全得跟着改。走系统浏览器之后
    **软件从头到尾看不见密码**，界面上只剩一个按钮。
83. **原生应用没有 client secret，也不该有。** 桌面程序里的任何「密钥」
    都能被扒出来。用 PKCE 顶替，而且只用 S256——plain 等于没有 PKCE。
    App ID 明文放在客户端里是正常的，那不是密钥。
84. **OIDC 这套流程「错了也能跑通」。** state 不校验、nonce 不校验、
    iss/aud 不查——每一条漏掉之后登录都照样成功，只是安全性没了。
    这种「失败不报错」的东西必须逐条写测试钉死，靠手动试是试不出来的。
85. **回调用回环不用自定义协议。** 免安装版可能注册不上协议（要写注册表），
    回环到哪儿都能用。端口写死三个备选全登记进白名单，占用了换下一个——
    直接失败的话作者只看到「登录不了」，根本不知道是端口被占了。
    用 127.0.0.1 不用 localhost：后者可能解析到 ::1 或被 hosts 改掉。
86. **客户端不验 id_token 签名是对的，服务端必须验。** 客户端是自己直接
    从令牌端点用 TLS 取的，不是从地址栏捡的；那台读统计的小服务收到的
    令牌来路不明，必须拿 JWKS 验并检查 aud。
87. **「关于你」和「账号」说的是同一件事。** 昵称在这页、连胜在那页，
    找起来没有道理。并成一页：上半截是本机的你（不登录也有），
    下半截是对外的你。
88. **浏览器那一趟在无头环境里验不了，就要说出来。** 协议那半有 44 个
    单元测试，进程间那段有冒烟，但「点一下真能登进去」得人工试一次——
    含糊地说「登录做完了」，等于把这个坑留给作者去踩。
89. **「登录服务器地址」这种框不该给用户。** 一旦有了，就等于给了别人
    一个可以骗他填的框——填成一台恶意 IdP，账号就交出去了，而界面上
    完全看不出异常。这类「一旦被利用后果很重、正常用户永远不会用到」
    的开关，正确做法是**写死在代码里**。换地址是改代码 + 重新打包的事，
    反正也要重新打包。
90. **改了代码不等于用户手上的包变了。** 作者截图报「账号还是服务器地址」，
    我差点去翻代码——其实代码早改了，他跑的是上一个 exe。
    以后凡是「界面没变」的反馈，先问版本号（这也是版本号要显眼的原因）。
91. **报上来的数先算一遍再改。** 作者说 DeepSeek 价目要改，六个数报过来，
    折算之后跟表里存的**一模一样**——表里存高峰价，闲时由 offPeakDiscount
    折半。真去「改」一遍等于假装干了活。改成加一组测试把这六个数钉住。
92. **回调页不看内容就说「登录成功」，是最坏的那种 bug。** 地址栏里
    明明是 `error=invalid_scope`，页面上写着登录成功，作者只能一脸茫然
    回到软件里看红条。**凡是「成功页」，都得先确认真的成功了。**
93. **别为一个还不存在的功能，把已有的功能搞挂。** 登录时要了
    `stats:write` 和 `resource=`，而那台读统计的小服务根本还没有、
    API 资源也没建，于是 Logto 直接 invalid_scope，**整个登录进不去**。
    等那个功能真做出来了再要这些权限。
94. **IdP 的原话要翻译成「该去改哪儿」。** `requested scope is not
    allowed` 甩给作者，他只会觉得软件坏了。现在会列出这次要了哪些权限、
    该去 Logto 检查哪几个开关。
95. **可有可无的权限，不该让整个登录进不去。** 第一次真连就栽在这儿：
    Logto 那边这个应用没被授予 `profile`，于是 invalid_scope，登录全挂。
    而 `profile` 只是用来显示昵称的。现在被拒了就去掉重来一次，
    退到只剩 openid 为止；退掉了什么在界面上说出来——
    不然作者会奇怪「为什么昵称是空的」。
96. **oidc-provider 报 invalid_scope 时，`scope` 参数里就写着是哪一个。**
    我一开始按经验猜是 offline_access，让作者白查了一遍开关。
    回调地址里明明写着 `scope=profile`——**证据就在眼前，别猜。**
97. **Logto 的「第三方应用」跟「原生应用」不是一回事。** 第三方应用默认
    只给 openid，别的权限要在「权限」页签里逐个授权。自己的软件建成
    第三方应用，就会莫名其妙缺权限。
98. **同一件事有两套协议时，删掉不用的那套。** 自建的邮箱+密码那一套
    （`core/account` 27 个测试、`account.ts`、一台假服务器）在换成 Logto
    之后已经没人调了，但它还在那儿，还导出着一个跟新模块同名的
    `PublicStats`。留着的唯一后果是：某天有人照着错的那份改。
    删了，git 历史里还在。
99. **「哪几个数会离开这台电脑」，两边都要显式列一遍。** 客户端挑一次、
    服务端再挑一次，拦的不是同一件事：服务端那道拦「别人乱塞」，
    客户端那道拦「我们自己不小心多带」。哪一边写成 spread，
    某天 planReport 里多个 `currentBook`，书名当场就飞出去——
    而且两边都不会报错。
100. **「有东西离开这台电脑」必须是作者按下的。** 所以推送有三道闸：
    登录了、认领了短名、自己打开了开关。自动上传**默认关着**，
    冒烟里有一条专门钉这个。这种事只要默认开着一次，
    作者就再也不信这个软件了。
101. **「会发出去什么」要在登录之前就看得见。** 那七个数一直摆在按钮
    上面，没登录时也摆着——「先看清楚再决定」不该需要先登录。
102. **给作者看的「别人读到什么」，必须走跟别人一样的路。** 所以那个
    按钮走的是公开接口、不带令牌，还原样吐 JSON 不美化。
    自己看自己的特权视图会骗人。
103. **做不到彻底删除的公开功能不该上线。** 「删掉服务器上的我」跟别的
    按钮一样显眼，不藏在二级菜单里——它是这整个功能敢开的前提。
104. **自动上传失败不该打断写作。** 记一句话，作者哪天翻到账号页会
    看见。为一次网络抖动弹个窗，是把我的焦虑转嫁给正在写字的人。
105. **发请求那一层要能不联网就测。** 把它单独拎成一个不 import electron
    的文件之后，「反代吐了 HTML 错误页」「超时」「令牌没带」这些
    只在真出事时才走到的路径，全都拿假 fetch 钉住了。
106. **「钉住」是个决定，不是个手势。** 钉住状态只活在内存里，
    关掉软件就散了 —— 作者钉上它是因为他要一直看着目录，
    每次开软件重钉一遍等于这个功能没做。**凡是作者「决定」出来的状态
    （钉住、互换、主题、开关），都该跨重启活着。**
107. **主题不是「白天/黑夜」两档。** 写字的人一天里换的是「眼睛累到什么
    程度」：下午刺眼要护眼绿，晚上关灯要真正的黑。两档不够用。
    颜色只在一个文件里定义一次，加一档就是加一条 —— 否则第三档一来，
    就得去几千行 CSS 里翻。
108. **借别人的生态，别翻译别人的生态。** Typora 主题直接原样注进去，
    只把稿纸容器的 id 定成 `#write`（它们全都写在这个选择器底下）。
    想「翻译」就得猜每份主题在干什么，猜错一次作者看到的是一团乱，
    还找不出是谁弄的。
109. **回执要跟动作一一对应。** 自动保存三秒一次，弹一次就是每三秒糊一次脸；
    而作者按 Ctrl+S 的那一下，本来就是在问「我刚才这一阵写了多少」。
    所以那个小弹窗只跟着手动保存走。
110. **浮层别进文档流。** 保存回执一旦占位，稿纸就往下顶一行，
    正在写的那行会跳 —— 比不弹还糟。fixed + pointer-events:none。
111. **菜单不该压住它要操作的东西。** 十几条的右键菜单从光标处往下开，
    正好盖住刚选中的字，右键复制时连「我选对了没有」都核对不了。
    躲的顺序也有讲究：先往右（选中那段还在原地），往上是最后一招。
    摆放算式抽成纯函数才测得了 —— 它一坏只能靠人眼盯。
112. **要恢复上次的位置，就得问一句。** 直接跳对「今天想开本新书」的人
    是劫持；每次从书架开始，对写到第八十章的人是每天点一遍目录。
    问一句，回车就是回去 —— 多数时候他就是想接着写。
113. **「有延迟」多半不是慢，是等错了信号。** 路线图等的是存盘，
    而存盘要停手三秒。把编辑器里那份还没存的正文直接交给建图，
    延迟就没了 —— 不用去优化任何一行建图代码。
114. **保留名会跟真名字撞。** `-> 结束` 当结局，直到有人给节点起名叫
    「结束」—— 然后整条线在跳过来的那一刻被掐掉，而他只看到
    「我后面写的都走不到」。**凡是保留名，都要有一个撞不上的显式写法**
    （`【…】`），保留名退成「没有同名节点时才生效」的兜底。
115. **「这一步只在一个地方写」比「这一步好写」更重要。** 分支合流原来要去
    每一条分支尾巴上补一行，五条分支五个地方，加一条还得记得回来补第六个。
    改成在合流处声明 `<- 甲、乙`，五条写在一处 —— 省的不是打字，
    是「以后会不会漏」。
116. **模板是教材，不是脚手架。** 教一次就够了，第二次是打扰 ——
    每新建一篇都塞一遍「李四」，作者得先删二十行才能开始写自己的东西。
    整份骨架只给第一篇，后面只给一行标题。空文件也不行：那样它连节点
    都算不上，图上不出现，作者会以为新建没成功。
117. **一个随机失败的测试比没有测试更糟。** 500 次真磁盘追加在 Windows 上
    并行跑会偶尔超时，那是机器忙不是代码坏 —— 但它会教人
    「红了先重跑一遍」，而那个习惯迟早会盖掉一个真 bug。
106. **往全局通道里塞东西之前，先问一句「谁还会听见」。** 目录树拖便利贴时
    顺手 `setData('text/plain', 卡片标题)`，理由是「Firefox 要求 setData
    才会开始拖」。但稿纸里坐着一个 CodeMirror，它认 text/plain ——
    于是作者拖一张人物卡出来，**标题被当成正文插进了他的稿子**，
    不报错、不提示。他还以为那是功能。自定义类型同样算 setData，
    那一行从头到尾就是多余的。
107. **同一个约定散在三处，就该抽成一个文件。** 上面那个修好之后，
    「拖便利贴」这件事在目录树、编辑器、稿纸三处都要用同一个 MIME 字符串。
    各写一遍的话，对不上的那天不会报错，只会让那个 bug 悄悄回来。
108. **砍功能时要想「已经在用它的人会怎样」。** 主题从五档砍到三档，
    `themeOf()` 原本是「认不出就退回纸白」—— 那意味着一个用惯墨灰的人
    升级之后打开软件，屏幕忽然一片惨白。加一张退休表让它退到最接近的
    同色系，两行的事。
109. **「每档记住字号」有个会互相污染的坑。** 换主题时字号是被这段逻辑
    自己改的，如果那一次也记一笔，就等于把新主题的字号写进旧主题里，
    来回切几次两档变成同一个数。所以「换主题那次不记」。
110. **自动替换必须能一次撤销回原样。** 打了个引号想反悔，Ctrl+Z 却把
    整段输入都撤掉 —— 人就再也不敢打字了。做法是分两笔：先正常插入他
    打的字符，再单独发一笔替换。这一条比功能本身还重要。
111. **默认值要按「大多数人怎么写」定，不是按「哪种更专业」定。**
    智能替换我一开始默认给了直角引号「」，那是照搬轻小说排版。
    作者一句话点破：「中文引号使用频率实则远高于直角引号」。
112. **净产出会是负的，而那不是没干活。** 「这一坐写了多少」如果只显示
    前后字数一减，改稿那天就是个负数，看着像一下午白干。三个数都摆出来：
    写了多少、删了多少、净多少。
113. **把排版挪进导出，比在稿子上做要干净得多。** 一度打算做「一键排版」
    直接往文件里塞全角空格 —— 那要破「文件里不存全角空格」这条，
    还带出「CSS 缩进跟真空格叠成四格」「排完要标记这篇已手动缩进」
    「撤销要整体退回」一串麻烦。**每一条都是自找的。**
    挪进导出之后：文件始终干净，排版只在产物里。
114. **已有功能漏了一种情况，跟「还没做的功能」不是一回事。**
    `renderBody()` 认得伏笔、双链、注释、Markdown 语法，唯独没管 `@` ——
    于是导给编辑的 txt 里行首那个 @ 是原样带出去的。这种漏子不会有人报，
    因为它看起来就像「本来就这样」。
115. **把构建输出丢进 /dev/null，等于把「构建失败」也丢掉了。**
    我在冒烟脚本的注释里写了一个反引号，而那整段是模板字符串 ——
    构建当场就挂了，但我看不见，于是那次冒烟跑的是**上一次的旧包**，
    还报了「276 步全过」。以后构建输出至少要 grep 一遍 error。
116. **测试因为错误的原因通过，比不测更坏。** 那条新加的冒烟写成了
    `book.meta.path`，而那段脚本里根本没有 `book` —— 它抛的是
    ReferenceError，于是「导出会被挡下来」这条**绿了**。
    断言要断在**报错的内容**上，不能只断「抛了没有」。
117. **`.cmd` 文件里一个非 ASCII 字节都不能有。** cmd.exe 按**系统 OEM
    代码页**（中文 Windows 上是 GBK）逐行读批处理文件，UTF-8 写的中文
    在那儿被解成乱码，然后它把乱码当命令去执行 —— 满屏「不是内部或外部
    命令」。`chcp 65001` 救不了，因为文件已经在被误读了。
    所以：给人看的中文放进 `.mjs`（node 读 UTF-8 没问题），
    批处理里只留 ASCII；连**传给 node 的那个文件名**也得是 ASCII，
    因为它同样要过 cmd 那一关。
    人点的那个 `.cmd` 自己可以叫中文 —— 资源管理器交给 cmd 的是真 Unicode。
118. **「能跑」和「能双击」是两回事。** 我交付一个命令行小工具时说的是
    「`node 那个.mjs` 跑一次试试」，而作者双击了那个 `.mjs`，
    Windows 拿记事本打开了它 —— 于是他的结论是「这玩意似乎是一个文本」。
    **交付一个工具，就得交付它的入口。**
